#include <stdio.h>
#include <pthread.h> 
#include <time.h>

	int main() {
	int i;
	while(1){
    time_t t;
    struct tm strukt;
    time(&t);
    localtime_r(&t, &strukt);
	printf("\r%2d:%2d:%2d", strukt.tm_hour, strukt.tm_min, strukt.tm_sec);
	fflush(stdout);
	sleep(1);
	}
}
